Balances:
Each User should have a list containing the index of transactions to limit number of linear searches.
Calculating total should just use index and search through the transaction table.

Settlement:
User reads outstanding balance page and pays the other user.
User creates a [settlement] transaction and input into transaction page.
